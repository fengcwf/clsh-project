# 阶段确认模板 / Phase Confirmation Templates

> **重要规则**: 代码示例必须独占一行，前后用空行隔开。不要将代码嵌入段落文本中。

---

## Phase 0: 项目初始化确认

```
✅ Phase 0 完成确认

项目名称: [PROJECT_NAME]
目录结构: [已创建]
PRODUCT.md: [已完成]
TECH.md: [已完成]
constitution.md: [已完成]
tasks.md: [已完成]

下一步: 进入 Phase 1 — 基础框架搭建
```

---

## Phase 1: 基础框架搭建确认

```
✅ Phase 1 完成确认

核心模块: [已创建]
配置管理: [已实现]
错误处理: [已实现]
单元测试: [已通过]

验证命令: [在此处列出验证命令，独占一行]
```

---

## Phase 2: 核心功能实现确认

```
✅ Phase 2 完成确认

功能模块 A: [已实现] [测试通过]
功能模块 B: [已实现] [测试通过]
不变量 INV-1: [已验证]
不变量 INV-2: [已验证]

端到端测试: [结果]
```

---

## Phase 3: 集成与优化确认

```
✅ Phase 3 完成确认

集成测试: [已通过]
性能指标: [达标/未达标]
边界条件: [已覆盖]
错误恢复: [已验证]

覆盖率: [百分比]
```

---

## Phase 4: 发布准备确认

```
✅ Phase 4 完成确认

所有不变量: [已验证]
所有用户故事: [已演示]
文档: [已更新]
版本号: [x.y.z]

准备发布: [是/否]
```

---

## 代码独占行规则 / Code On Its Own Line Rule

在所有文档中，代码必须遵循以下格式：

**正确** ✅

运行以下命令来验证安装：

```bash
[command here]
```

安装成功后，你会看到：

```json
{"status": "ok"}
```

**错误** ❌

运行 `[command here]` 来验证安装，你会看到 `{"status": "ok"}`。

---

## 确认格式规范

1. 每个阶段确认使用 ` ``` ``` ` 代码块包裹
2. 状态标识使用 ✅ (完成) 或 ❌ (未完成)
3. 验证命令必须独占一行
4. 确认必须列出所有检查点
